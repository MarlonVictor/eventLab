query GetLessonBySlug ($slug: String) {
    lesson(where: {slug: $slug}) {
        description
        title
        videoId
        availableAt
        teacher {
            avatarURL
            bio
            name
        }
        challenge {
            url
        }
    }
}